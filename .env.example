# --- Firebase Configuration ---
# You need to provide Firebase credentials. Choose ONE of the following methods:

# Method 1: For local development (using a service account key file)
# Provide the path to your Firebase service account JSON key file.
# Download this file from your Firebase project settings > Service accounts.
# Example: FIREBASE_SERVICE_ACCOUNT_KEY_PATH=./path/to/your-service-account-key.json
FIREBASE_SERVICE_ACCOUNT_KEY_PATH=

# Method 2: For deployment platforms like Vercel (using a JSON string)
# Paste the entire content of your Firebase service account JSON key file here.
# This is often used when you can't upload files directly but can set environment variables.
# Make sure the JSON is properly escaped if necessary for your environment.
# Example: FIREBASE_SERVICE_ACCOUNT_JSON={ ... your service account json content ... }
FIREBASE_SERVICE_ACCOUNT_JSON=

# --- TMDB API Key ---
# Required to fetch movie and series details (title, poster).
# Get a free API key from https://www.themoviedb.org/settings/api
TMDB_API_KEY=

# --- Server Configuration (Optional) ---
# Port the server will run on locally. Defaults to 5000 if not set.
PORT=5000

# --- Cache Configuration (Optional) ---
# Time-to-live for cached items in seconds. Defaults to 3600 (1 hour) if not set.
CACHE_TTL=3600
